{
  "name": "nebula-ausp-fresh",
  "private": true,
  "version": "0.13.0",
  "type": "module",
  "homepage": "https://MadGoatHaz.github.io/nebula-ausp",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm version minor --no-git-tag-version && vite build && gh-pages -d dist && git push --follow-tags",
    "server": "node server.js"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "lil-gui": "^0.19.2",
    "three": "^0.164.1"
  }
}
